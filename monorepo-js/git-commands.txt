1. mkdir monorepo-js # to create directory monorepo-js 
2.  Used touch README.md    #to create README.md file
    Used touch .gitignore   #to create .gitignore file
    Used touch LICENSE      #to create LICENSE file

    echo "file content" >> filename #to print file content

    git add .    # to add/track files
    git commit   #to commit the file

3. mkdir backend   # to create backend directory
   touch .gitkeep  # to create empty .gitfile
   git add .
   git commit      #to commit added files

4. git remote add origin "git@github.com:imaani1/monorepo-js.git"

5. git push -u origin master

6. git checkout -b fronted-layout

7.mkdir src
    mkdir components
      cd components 
      touch .gitkeep

    mkdir containers
       cd components 
       touch .gitkeep
   git add .
   git commit -m "chore: add components and containers directory to frontend"
   
8.  touch index.js
     echo" content ">> index.js

    git add index.js
    git commit -m""

9. git push

10. git merge fronted-layout

11. git checkout -b public # to create new branch public
    touch index.html
    echo "content " >> index.html
    git add .
    git commit -m""

12. git push
13. git checkout -b commands
     







